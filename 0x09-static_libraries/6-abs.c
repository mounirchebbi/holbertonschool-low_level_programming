#include "holberton.h"
/**
  *_abs - get the abs value of character
  *@i : character to abs
  *Return: the abs value
  */
int _abs(int i)
{
	if (i < 0)
	{
		i = i * -1;
	}
	else
	{
	}
	return (i);


}
